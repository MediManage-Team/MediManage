# Phase 0 governance defaults
ai.assistant.enabled=true
# Future roadmap modules remain off until explicitly enabled
subscription.release.enabled=false
subscription.commerce.enabled=false
subscription.approvals.enabled=false
subscription.discount.overrides.enabled=false

# Subscription pilot rollout controls (evaluated only when pilot is enabled)
subscription.pilot.enabled=false
subscription.pilot.allowed.usernames=
subscription.pilot.allowed.user_ids=
